{
  "name": "claims-processing-system",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Insurance claims processing system — adjudication, lifecycle, disputes",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
